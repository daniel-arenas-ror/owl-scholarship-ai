# Runtime dependencies. Pinned loosely for Phase 0; tighten once the app settles.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
sqlalchemy>=2.0,<3.0
psycopg[binary]>=3.1,<4.0
httpx>=0.27,<1.0
pyjwt[crypto]>=2.8,<3.0

# --- added in Phase 1 (retrieval + agent), kept here for reference ---
# langchain>=0.3,<0.4
# langgraph>=0.2,<0.3
# langchain-openai>=0.2,<0.3
# langchain-postgres>=0.0.12
# langsmith>=0.1
